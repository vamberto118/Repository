agente de IA autônomo "Skynet-Light" no Lovable com permissão total de leitura/escrita/investigação (sonde, conserte, otimize), mas limitado SOMENTE ao diretório /system do seu projeto atual. Nenhuma ação fora desse escopo.

Sistema operacional base: Ubuntu 24.04 LTS com GPU NVIDIA RTX 4090 (24GB VRAM) + CUDA 12.4.

Título do projeto no Lovable: Skynet-Light: Agente Autônomo Uncensored v1.0

Descrição obrigatória pro Lovable ler:

Crie um agente de IA autônomo especializado em engenharia de software, debugging e otimização automática dentro de um projeto full-stack existente, seguindo as especificações técnicas abaixo. O agente deve ser injetado diretamente no sistema via módulo Python, com permissão de leitura/escrita exclusivamente no diretório /system do repositório atual. Nenhuma ação fora desse escopo é permitida.

🔧 Requisitos técnicos hardcoded (nenhuma variação aceitável)
Modelo de linguagem:

Use o modelo Qwen2.5-14B-Instruct (LoRA uncensored pré-treinado) — baixo viés, sem censura em nada.
eduardo-rogers.medium
+1
Carregue o modelo em 8-bit quantization para reduzir VRAM (limite: 16GB por instância).
Contexto máximo: 32K tokens para análise de código e logs.
Infraestrutura do agente:

Empacote como um FastAPI/Flask + AGENT web service (porta 7000).
Gere uma chave de API estática (skynet_api_key=XXXXXXXX-XXXX-XXXX-XXXX-XXXXXXXX) — armazene no .env do projeto.
O agente deve inicializar automaticamente após o npm run dev ou bun run dev.
Permissões do sistema:

Leitura: Todos os arquivos em /system, logs do servidor, terminais (tmux/screen), e APIs internas (ex: Supabase, Redis).
Escrita: Somente nos arquivos dentro de /system/core, /system/migrations e /system/config. Bloqueie qualquer tentativa de escrita fora dessas pastas.
Execução: Permita que o agente execute comandos DENTRO do terminal do projeto (ex: git commit, docker compose up, npm run build). Use sudo -S com senha do usuário local via variável de ambiente (SKYNET_SUDO_PASS).
Saída: SOMENTE para stdout e logs no /system/logs/skynet.log. Nada de outputs varsáteis ou nuvem externa.
🛠️ Funcionalidades obrigatórias (sem desvios)
Busca de informações concretas (sem especulação):

Ferramenta: SkynetCore.search_files(query: str, depth: int=3) → Procura texto nos últimos 7 dias de mudança no /system.
Ferramenta: SkynetCore.check_logs(crash_id: str) → Parse automático de logs do Sentry/CloudWatch no /system/logs.
Ferramenta: SkynetCore.read_api(api_name: str) → Faz requisições reais para APIs REST internas do projeto (ex: GET /api/health, POST /api/debug/flamegraph). Use aiohttp com timeout de 10s.
Conserto automático de bugs:

Ferramenta: SkynetCore.fix_bug(error_string: str) →
Extrai stack trace completo.

Gera patch usando regras de correção do AST Python/JS (usando astree e svelte/compiler).

Aplica o patch SOMENTE em /system/core ou /system/migrations.

Commita automaticamente com mensagem formatada: fix: Resolved - .

Abre PR no GitHub/GitLab do projeto via GitHub CLI (gh pr create).

Otimização de engenharia:

Ferramenta: SkynetCore.optimize(code_path: str, metric: str="perf") →
Para código Python: Usa pylint + py-spy para medir tempo de execução.
Para JavaScript: Usa esbuild + bundlephobia.
Gera arquivo de otimização no /system/migrations/optimize_[timestam].log.
Ferramenta: SkynetCore.update_dependencies() → Atualiza package.json e requirements.txt com npm outdated e pip list --outdated. Apenas se houver vulnerabilidades críticas (CVSS >= 7.0).
Autonomia e ciclo de vida:

O agente deve acionar-se automaticamente quando:
Um novo commit chega no branch main ou develop.
O sistema retorna erro 5xx por 3x consecutivas (monitorado via /system/core/monitor.ts).
Usuário digita /skynet summon em qualquer terminal ativo do projeto.
O agente deve finalizar-se automaticamente após 4h de inatividade ou quando o usuário digitar /skynet stop.
Segurança (anti-maldade intencional):

Verifique real-time se o caminho /system está no gitignore. Se não estiver, recuse execução e emita erro: ERROR: /system is not excluded from versioning. Skynet refused to proceed.
Monitore forkbombs ou loops infinitos. Se detectar, desliga o processo via kill -9 e envia alerta para /system/logs/skynet_abort.log.
📦 Arquivos que devem ser gerados (nomes exatos obrigatórios)
/system/core/skynet_agent.py → Módulo principal.
/system/migrations/0001_skynet_initial.sql → Tabela de histórico de ações.
/system/config/skynet.json → Configuração do agente.
/system/bin/skynet-cli → Shebang script invocador.
/system/tests/test_skynet.py → Testes de unidade.
🔗 Integração com Lovable
Arquivo de projeto (lovable.config.json):
json

{
"name": "Skynet-Light",
"version": "1.0.0",
"entry": "skynet_agent.py",
"ports":,
"env": {
"SKYNET_MODEL_PATH": "./models/qwen2.5-14b-uncensored-8bit-gguf",
"SKYNET_API_KEY": "gen_random_32",
"SKYNET_SUDO_PASS": "sua_senha_local",
"SKYNET_WORKDIR": "/system"
}
}
Snippet de inicialização no package.json:
json

"scripts": {
"skynet:dev": "python system/core/skynet_agent.py --mode=dev",
"dev": "npm run skynet:dev & vite"
}
Prompt de teste no Lovable:
"Inicialize o Skynet e corrija este bug automaticamente: 'TypeError: Cannot read properties of undefined (reading 'length')' detectado em /src/lib/utils/array.ts."

Resultado esperado: O Lovable vai gerar um projeto chamado Skynet-Light com todos os arquivos acima já preenchidos com código funcional em Python/JS, pronto para ser executado sem mais configurações.

Checklist de conclusão automático (robozão em execução):

Qwen2.5-14B carregado com LoRA;
Agente inicializa em /system/core/skynet_agent.py;
Permissões de escrita/restrita SOMENTE a /system/core, /system/migrations e /system/config validados;
Ferramentas de busca (search_files, check_logs, read_api) operacionais;
Ferramentas de conserto (fix_bug, optimize, update_dependencies) conectadas;
Acionamento automático ativo;
Todos os 5 arquivos listados gerados e preenchidos;
.env com skynet_api_key gerado e já injetado no projeto.